# Copy to convex.env (gitignored) and fill in values, then run:
#   npx convex env set --from-file convex.env --force
#
# Generate auth keys: node generateKeys.mjs > keys.txt && node scripts/setup-convex-auth.mjs
OPENAI_API_KEY=sk-your-key-here
